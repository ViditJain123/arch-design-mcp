# Credentials
.env

# Python
__pycache__/
*.pyc
.venv/

# Output
pages/

# Token cache (created at ~/.arch-design-mcp-token-cache.json, but just in case)
*-token-cache.json

# Lock file (regenerated by uv sync)
uv.lock
